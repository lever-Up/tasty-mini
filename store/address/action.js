import { actionCreator } from '../tool';
// import QqMapWx from '../../utils/location';

// 获取地理位置
  export const GET_LOCATION = 'GET_LOCATION';
  export const getLocation = function () {
    return {
      type: GET_LOCATION,
    }
  };

{
  const UPDATE_LOCATION = 'UPDATE_LOCATION';
}
